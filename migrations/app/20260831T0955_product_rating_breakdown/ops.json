[
  {
    "id": "column.public.product.ratingBreakdown",
    "label": "Add column \"ratingBreakdown\" to \"product\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "ratingBreakdown",
        "table": "product"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"ratingBreakdown\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "ratingBreakdown"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"ratingBreakdown\"",
        "sql": "ALTER TABLE \"public\".\"product\" ADD COLUMN \"ratingBreakdown\" json"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"ratingBreakdown\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "ratingBreakdown"
        ]
      }
    ]
  }
]